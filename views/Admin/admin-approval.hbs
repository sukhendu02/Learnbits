<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/lucide@latest/dist/umd/lucide.min.js" rel="stylesheet">
  <style>
    body {
      font-family: sans-serif;
      padding: 2rem;
      background-color: #f9fafb;
    }

    .dashboard {
      max-width: 800px;
      margin: auto;
    }

    .filter-buttons {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
    }

    .filter-buttons button {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 8px;
      background-color: #e0e0e0;
      cursor: pointer;
    }

    .filter-buttons button.active {
      background-color: #4f46e5;
      color: white;
    }

    .blog-card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      padding: 1.25rem;
      margin-bottom: 1.5rem;
    }

    .blog-title {
      font-size: 1.2rem;
      font-weight: bold;
      color: #333;
      margin-bottom: 0.5rem;
    }

    .blog-status {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 1rem;
    }

    .actions {
      display: flex;
      gap: 0.75rem;
      margin-top: 0.5rem;
    }

    .actions button {
      padding: 0.4rem 0.75rem;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9rem;
    }

    .approve { background-color: #22c55e; color: white; }
    .reject { background-color: #ef4444; color: white; }
    .feedback-input {
      margin-top: 0.75rem;
    }

    .feedback-input textarea {
      width: 100%;
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 0.5rem;
    }
  </style>
</head>
<body>

<div class="dashboard">
  <h2>Admin Blog Dashboard</h2>

  <!-- Filter Buttons -->
  <div class="filter-buttons">
    <button class="active">All</button>
    <button>In Review</button>
    <button>Approved</button>
    <button>Rejected</button>
  </div>

  <!-- Blog Card (Repeat for each blog in JS) -->
  <div class="blog-card">
    <div class="blog-title">How to Ace JavaScript Interviews</div>
    <div class="blog-status">Status: In Review</div>
    
    <div class="actions">
      <button class="approve">Approve</button>
      <button class="reject">Reject</button>
    </div>

    <div class="feedback-input">
      <label for="feedback">Feedback for Rejection</label><br />
      <textarea id="feedback" rows="3" placeholder="Write feedback for the author..."></textarea>
    </div>
  </div>

  <!-- Add more blog cards here dynamically -->
</div>

</body>
</html>
